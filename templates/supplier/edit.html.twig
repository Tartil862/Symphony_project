{% extends 'base.html.twig' %}

{% block title %}{{ 'supplier.edit'|trans }}{% endblock %}

{% block page_title %}{{ 'supplier.edit'|trans }}{% endblock %}

{% block body %}
    <div class="mb-4">
        <a href="{{ path('app_supplier_index') }}" class="btn btn-ghost">
            <i class="bi bi-arrow-left me-1"></i> {{ 'supplier.title'|trans }}
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card mb-4">
                <div class="card-header">
                    <i class="bi bi-pencil-square me-2"></i>{{ 'supplier.edit'|trans }}
                </div>
                <div class="card-body p-4">
                    {{ include('supplier/_form.html.twig', {'button_label': 'common.update'|trans}) }}
                </div>
            </div>
            <div class="text-end">
                {{ include('supplier/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
